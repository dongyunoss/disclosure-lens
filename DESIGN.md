# 공시렌즈 디자인

차콜과 무채색 표면을 중심으로 공시 내용과 수치에 집중하는 리서치 화면을 구성한다.

- 배경 #141414, 사이드바 #191919, 카드 #1d1d1d. 기본 문자 #f0f0f0, 보조 문자 #b0b0b0 계열.
- 강조색은 채도를 낮춘 슬레이트 블루 #a8b9d0 한 가지로 제한한다. 선택 표시·링크·키보드 포커스에 사용한다.
- 그라데이션, 배경 광원, 점무늬, 카드별 장식 색상을 사용하지 않는다. 계층은 명도·여백·테두리로 구분한다.
- 검토 상태 숫자와 배지는 무채색으로 통일하고 상태 문구로 의미를 전달한다. 음수와 오류에는 낮은 채도의 붉은색을 보조적으로 사용한다.
- 실제 PDF는 흰 종이와 원래 픽셀을 유지한다. 이전 셀은 회색 점선, 이후 셀은 슬레이트 블루 실선으로 구분한다.
- 데스크톱 사이드바와 모바일 5개 메뉴 구조를 유지한다. 카드·버튼 여백과 모서리를 유지하고 숫자는 tabular-nums를 사용한다.
- 키보드 포커스는 배경에서 식별되는 외곽선으로 표시한다. reduced-motion 설정을 따르고 외부 리소스 요청을 추가하지 않는다.

공통 표현은 src/theme.css에서 관리하며 각 화면 스타일 다음에 불러온다. 디자인 변경은 데이터·분석 범위·기능 제공 상태를 변경하지 않는다.
