# 공시렌즈 디자인

사용자가 제공한 참고 이미지의 짙은 차콜, 둥근 카드, 청록·보라 빛을 재무 리서치 화면에 적용한다. 참고 이미지의 로고·인물·문구는 서비스에 사용하지 않는다.

- 배경: #111215. 카드: #1c1d23–#24252b. 기본 문자: #f0f1f7. 보조 문자: #b1b3c1.
- 강조: 청록 #84dfd6, 보라 #b5a4ff. 선택 상태는 테두리·배경·아이콘과 텍스트 상태를 함께 사용한다.
- 그라데이션은 제목 영역과 소수의 강조 카드에 사용한다. 수치·표 셀에는 장식 효과를 넣지 않는다.
- 데스크톱은 여백을 둔 둥근 사이드바, 모바일은 브랜드 아래 5개 메뉴를 배치한다.
- 카드 모서리는 18–28px, 입력·버튼은 10–13px. 숫자는 tabular-nums를 사용한다.
- 검토 상태는 확인 우선(살구색), 주요 변화(청록), 미해당(연보라), 보류(회색)로 구분하며 기존 의미를 유지한다.
- 실제 PDF 표는 흰 종이·원래 픽셀을 유지한다. 반전·색상 필터·투명도를 적용하지 않는다. 원문 위의 좌표 강조와 확대는 그대로 유지한다.
- 대비가 낮은 장식 문자를 데이터나 상태 표시에 사용하지 않는다. 키보드 포커스는 밝은 보라 외곽선으로 보인다.
- 효과는 짧은 색 변화만 사용하며 reduced-motion 설정을 따른다. 외부 폰트·이미지·스크립트 요청은 추가하지 않는다.

공통 표현은 src/theme.css에서 관리하며 기본 구조와 각 화면의 기능 스타일 위에 마지막으로 불러온다. 기업 검색 및 주문형 분석 기능의 구현·운영 승인은 별도이며, 이 시각 디자인 변경은 그 기능이 제공된다는 표시를 추가하지 않는다.
